# Which Optipeople API the Vite dev proxy forwards /auth-api/* to.
# Copy this file to .env.local and uncomment the one you want.

# Staging (default if unset)
#VITE_API_TARGET=https://api-staging.optipeople.dk

# Production
#VITE_API_TARGET=https://api.optipeople.dk
